"""
Main entry point for Symphony Core Document Management Workflow CLI.

This module provides the command-line interface for document validation,
conflict detection, and auto-fixing.
"""

from src.cli import cli


if __name__ == '__main__':
    cli(obj={})
